This project uses Typescript, Material UI, React Query, Javascript, and React. It also uses the Fake Store API.

This will be updated using more TypeScript types as I continue to learn more about TS.
